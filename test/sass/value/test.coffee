describe 'sass/value >>', ->
  beforeEach -> this.filename = __filename

  it.skip '0', -> this.shouldBeOk()
  it.skip '1', -> this.shouldBeOk()
  it.skip '2', -> this.shouldBeOk()
  it.skip '3', -> this.shouldBeOk()
  it.skip '4', -> this.shouldBeOk()

  it.skip 'dimension.0', -> this.shouldBeOk()
  it.skip 'dimension.1', -> this.shouldBeOk()
  it.skip 'dimension.2', -> this.shouldBeOk()

  it.skip 'rgb.0', -> this.shouldBeOk()
  it.skip 'rgb.1', -> this.shouldBeOk()
  it.skip 'rgb.2', -> this.shouldBeOk()

  it 'vhash.0', -> this.shouldBeOk()
  it 'vhash.1', -> this.shouldBeOk()
  it 'vhash.2', -> this.shouldBeOk()
  it 'vhash.3', -> this.shouldBeOk()
